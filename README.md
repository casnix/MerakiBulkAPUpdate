# MerakiBulkAPUpdate
Make configuration updates to batches of Meraki APs

## Command usage:
waps.py --token API-TOKEN
--claim - claim without adding to network
--add - claim and add to networks
--update - update names/tags/etc
--source - source CSV
